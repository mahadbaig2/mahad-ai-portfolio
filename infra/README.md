# infra

Infrastructure manifests, Dockerfiles, and deployment definitions.

- `docker-compose.yml`: Local PostgreSQL and Qdrant development containers.
- Deployment specs for Cloudflare Pages and Hugging Face Spaces (CPU Basic).
